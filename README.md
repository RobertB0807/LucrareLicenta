# LucrareLicenta

## Run the full app

From repo root, start backend + mobile app together:

```bash
chmod +x run-all.sh
./run-all.sh
```

To also start the web interface from `interfata/cyber-sentinel`:

```bash
./run-all.sh --with-web
```
